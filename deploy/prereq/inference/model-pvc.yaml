apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen36-model-cache
  namespace: llm-inference
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 80Gi
